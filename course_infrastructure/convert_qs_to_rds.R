# Deprecated wrapper — use course_infrastructure/convert_eviction_data.R (writes .rds and .qs2).
message("Note: use course_infrastructure/convert_eviction_data.R; running it now.")
source("course_infrastructure/convert_eviction_data.R")
